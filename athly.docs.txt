
IMPLEMENT SKILLS
IMPLEMENT testing
understand /routes
structure the plan so each muscle group gets hit
comfortable with heavier  weights is not set
the workouts are not structured properly(are enough sets calculated per week?)
add progression for 2x to faliour




3. Form Tips RAG — During Session Coaching ⭐ High Value
Your formTips field on each exercise is currently just stored data. With RAG you can make it conversational:
User: "my back hurts during this deadlift"
                ↓
Retrieve deadlift node → formTips metadata
                ↓
LLM reasons over the tips + user complaint
                ↓
"Make sure your back is flat, not rounded. 
 Try reducing weight by 20% and focus on hip hinge..."
This turns static form tips into real-time coaching.

3. Conversational Program Design ⭐⭐ LangChain
Your agent currently picks a split (PPL, Upper/Lower, Arnold) and generates it once at registration. A more powerful feature is a multi-turn conversation that refines the program:
User: "I want to switch from PPL to Upper/Lower"
Agent: "You've been on PPL for 8 weeks. Your push day volume 
        is higher than pull — want me to rebalance that in 
        the new split?"
User: "yes but keep my bench and OHP"
Agent: "Got it. Here's your new Upper/Lower keeping bench 
        on Upper A and OHP on Upper B..."

**User questions - Goes into the schema, also update the exercise schema to match the zod schema







**LLM USAGE
Metric	        "Good" Target	Danger Zone	    How to Fix					
p50 Latency	    2s	            > 5s	        Reduce output tokens; switch to a faster model(GeminiFlash).					
p99 Latency	    8s	            > 15s	        Check for DB timeouts or retry logic that is too aggressive.					
Input Tokens	1k	            > 8k	        Filter user history before sending; use RAG (Vector Search).					
Output Tokens	200	            > 600	        Ask for JSON only; remove "chatty" intros/outros.					
								




***PAST WEEK 8
gcloud auth login-?
10.Fix how the user id is fetched in functions, it should be fetched from auth currently it is just expected as a parametar, fix this once you are done testing
1. Ton of fixing and work to do around the auth and jwt, token isnt stored in database and refresh token isnt implemented at all, revert to easy-reservartion project and sync the auth.
2. integrate llamaindex
3. kao so vlaga userot prvpat, mu sugeriras splitovi, moze i costum split da si napraj
4. si stava kolku pati nedelno saka da vezba ne se bitni denovite nego progress ke gleame po nedeli
16. when user turns from beginner to intermediate, create new workouts
17. Figure out how to progress with beginners most efficiently
18. Figure out what exactly can be deleted after a certain time period and set more TTL indexes
19. If a user takes a 3 month break, it should announce to the agent, agent will send the records. User can paste the records when it comes back, TTL index on workouts stays the same.
20.-After the workout is submitted the user is asked 2 questions, whether he would like to change any exercise and whether the weight is appropriate(we just need to ask the question)
21. Ask user which muscle group would he like to prioritize?
22. Introduce supersets
23. make a good use of this workoutFrequencyPerWeek
11. Have a warm-up for heavier movements
3. tools for suggesting the starting weight, sets and reps.
14. test for injuries
15. keep the injuries spotted in context
16. before each workout, ask for soreness or fatigue or lack of energy
17. track workouts with calendar and enforce rest days if needed or plan the workout days ahead and remind with a nortification
appendSamplingAuditLog writes to localStorage on each sampling request.
If request volume grows, batching/throttling logs is possible.
INSTRUCTIONS
18. Authorization (if HTTP) : Who can call which tool?; Rate limiting?; Auth headers?
19. In registry.ts the tools and prompts are stored in memmory and it may be a performance risk as the app scales3
20. Automatically detect if a session is expired



bash gcloud auth login
$ bash gcloud config set project project-e01e21dc-8855-4d6d-a22
$ bash gcloud auth application-default login 









G90ISHFIS1231!2323g